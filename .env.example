# Remote Ollama server (no trailing slash, no /v1 suffix)
OLLAMA_BASE_URL=http://your-remote-host:11434

# Model name must match `ollama list` on the remote host
OLLAMA_MODEL=llama3.1:8b

# Negotiation defaults
MAX_NEGOTIATION_ROUNDS=10
MAX_INTAKE_FOLLOWUPS=5
