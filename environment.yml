name: loan-negotiation
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - autogen-agentchat>=0.4.0
      - autogen-ext[ollama]>=0.4.0
      - httpx>=0.27
      - pydantic>=2.0
      - pydantic-settings>=2.0
      - python-dotenv>=1.0
      - typer>=0.12
      - pytest>=8.0
      - ruff>=0.6
      - -e .
